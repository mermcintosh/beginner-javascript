const p = document.querySelector('p')
console.log(p)

const divs = document.querySelectorAll('div')
console.log(divs)

//node list is not an array. it is a list, but it doesn't have all
//the methods that arrays have

//console.dir('h2') <-- will share all the info about
//the object (the heading)



//data attributes

